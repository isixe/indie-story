---
import { getAllPosts } from '../../utils/posts';

export function getStaticPaths() {
  const allPosts = getAllPosts();
  return allPosts.map((post) => ({
    params: { slug: post.slug },
  }));
}

const { slug } = Astro.params;
return Astro.redirect(`/zh/post/${slug}`, 301);
---